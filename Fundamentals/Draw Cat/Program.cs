using System;

namespace Draw_Cat
{
    class Program
    {
        static void Main()
        {
            int size = int.Parse(Console.ReadLine());
            char symbol = char.Parse(Console.ReadLine());

            for (int i = 0; i < size; i++)
            {

            }
        }
    }
}
